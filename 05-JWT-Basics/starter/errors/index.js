const UnauthenticatedError = require("./unauthenticated");
const CustomAPIError = require("./unauthenticated");
const BadRequest = require("./bad-request");

module.exports = { UnauthenticatedError, CustomAPIError, BadRequest };
